[package]
name = "network-engine-worker"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"

[dependencies]
network-engine = { path = "../network-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["raw_value"] }
uuid = { version = "1", features = ["serde"] }
tracing = "0.1"
tracing-subscriber = "0.3"
tracing-log = "0.2"
chrono = { version = "0.4", features = ["clock"] }

[dev-dependencies]
# Used only to emit a `log`-crate record in the signal-layer bridge test, so the
# log->tracing normalization path stays covered after the engine dropped `log`.
log = "0.4"
