[package]
name = "network-engine"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"

[dependencies]
uuid = { version = "1", features = ["v4"] }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
proptest = "1"
serde_json = "1"
