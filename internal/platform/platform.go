// Package platform provides infrastructure services consumed by all bounded
// contexts: configuration, audit logging, job scheduling, and session management.
// Platform is built first to unblock all other contexts.
package platform
