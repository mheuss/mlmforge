jobs:
  go:
    steps:
      - uses: actions/setup-go@v5
        with:
          go-version: "1.27.1"
      - if: always()
        uses: actions/setup-go@v5
        with:
          go-version: "9.9.9"
